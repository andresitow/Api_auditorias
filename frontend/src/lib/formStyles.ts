/** Clases de Tailwind compartidas por los formularios. Antes cada modal/paso las
 * redefinía como constantes locales idénticas. */

/** Campos de los modales y páginas de configuración de auditorías (compacto). */
export const inputCls = "w-full bg-bg3 border border-border text-text rounded-md px-2.5 h-9 text-[13px] outline-none focus:border-blue";
export const labelCls = "text-[11px] uppercase tracking-wide text-muted mb-1 block";

/** Campos de los pasos del asistente de formularios (más grandes). */
export const stepInputCls = "w-full bg-bg3 border border-border text-text rounded-md px-3 h-10 text-sm outline-none focus:border-blue";
export const stepLabelCls = "block text-xs text-muted mb-1.5 font-medium";
